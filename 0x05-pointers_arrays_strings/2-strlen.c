#include "main.h"

/**
 * _strlen- prints string length.
 * @s: string whose length is to be determined
 * Return: Integer
 *
 */
int _strlen(char *s)
{
	int length = 0;
	int i;
	char a[];

	for (i = 0; a[i] != '\0'; i++)
	{
		length++;
	}
	return (length);
}
